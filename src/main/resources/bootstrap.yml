spring:
  application:
    name: licensing-service
  profiles:
    active: test             # тут указывается тот профиль (dev/test), на котором будет собираться проект(в зависимости от профиля подтягиваются разные настройки)
  cloud:
    config:
      uri: http://localhost:8071

management:
  security:
    enabled: false